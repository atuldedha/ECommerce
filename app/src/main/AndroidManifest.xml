<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_commerce">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".otp.OTPverificationActivity"></activity>
        <activity android:name=".view_all.ViewAllActivity" />
        <activity android:name=".my_addresses.MyAddressesActivity" />
        <activity android:name=".add_address.AddAddressActivity" />
        <activity android:name=".delivery.DeliveryActivity" />
        <activity android:name=".order_details.OrderDetailsActivity" />
        <activity android:name=".product_details.ProductDetailsActivity" />
        <activity android:name=".category_activity.CategoryActivity" />
        <activity
            android:name=".main_screen.StarterActivity"
            android:label="@string/title_activity_starter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".login_and_register.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>